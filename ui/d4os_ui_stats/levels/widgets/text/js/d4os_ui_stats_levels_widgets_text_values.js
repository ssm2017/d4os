function drawChart() {}
function setTags() {
  $("#par-uptime").text(Uptime);
  $("#par-ragent").text(RootAg);
  $("#par-version").text(Version);
  $("#par-cagent").text(ChldAg);
  $("#par-simfps").text(SimFPS);
  $("#par-phyfps").text(PhyFPS);
  $("#par-memory").text(Memory);
}